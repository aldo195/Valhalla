#raid
    md-card#raid-card
        md-card-title
            md-card-title-text
                span.md-headline Valhalla.io  |  Launch Raid
        md-card-content
            md-list(flex)
                md-list-item
                    md-input-container
                        label Select a raid
                        md-select(ng-model="raidVm.selected.target", ng-change="raidVm.createRaid()")
                            md-option(ng-repeat="target in raidVm.targets", ng-value="target")
                                | {{ target.name }}
            md-list-item
                a(ng-href='{{ raidVm.downloadURL }}', target="_blank")
                    p Click to download
            md-list-item
                p Enter the active raid's trophy value once your team finds it:
                br
                br
                input(ng-model="raidVm.trophy", valhalla-enter="raidVm.finishRaid()")
            md-list-item
                label {{ raidVm.elapsedTime == 0 ? '': 'Raid lasted ' + raidVm.elapsedTime + ' seconds.' }}